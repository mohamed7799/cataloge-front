import { makeStyles } from "@material-ui/core";

const useStyle = makeStyles({
  root: {
    minHeight: "100vh",
    padding: "1rem",
  },
  loadingIcon: {
    display: "block",
    margin: "2rem auto",
  },
});

export default useStyle;
